# PROJECT_STATE

Last updated: 2026-07-04

## Current Status

- Argos is a Python 3.12+ terminal coding agent with active package code in `argos/`.
- Global and project Codex workflow rules now support natural-language task routing, native `/plan`/`/goal`/`/review`/`/init` commands, safe optional subagent use, and PR/conflict boundaries without requiring "做：" or "目标：" prefixes.
- 2026-07-03 dynamic-workflow read-only analysis used an `explorer` subagent plus local inspection. No business code was changed.
- 2026-07-03 dynamic-workflow product closeout fixed several `ARGOS_CONFIG_DIR` default-path drifts: MCP config, LSP prompt injection gating, external-surface warnings, skills user dir/cache, Dream candidates, Dream CLI roots, conductor Dream material gate, and permissions audit logs now resolve through the configured Argos root unless explicitly injected.
- `argos --selftest` now verifies with the current Python interpreter when safe, and falls back to `python3` for frozen/PyInstaller binaries where `sys.executable` is the Argos binary.
- Dream CLI startup now distinguishes missing-key fallback from real initialization failures; malformed config/runtime errors return failure instead of running memory-only mode.
- 2026-07-03 ship-readiness checkpoint fixed TUI daemon auto-spawn pid handling: `probe_or_spawn()` now passes `--pid-path` beside the selected socket path, so stale-daemon cleanup reads the same pid file the spawned daemon writes.
- Daemon socket defaults now follow `ARGOS_CONFIG_DIR/daemon.sock` when `ARGOS_DAEMON_SOCKET` is not explicitly configured; TUI daemon probing and `argosd` CLI defaults use the same source.
- CONTRIBUTING and README now describe daemon socket state under the Argos config directory instead of hard-coding `~/.argos/daemon.sock`.
- TUI setup and external-capability hints now show the active `ARGOS_CONFIG_DIR` paths for `config.json`, `.env`, `hooks.json`, `lsp.json`, and `mcp.json` instead of hard-coded `~/.argos/...` paths.
- CLI setup help, paste-key storage warnings, and non-TTY setup fallback instructions now show the active `ARGOS_CONFIG_DIR` files for `config.json` and `.env`.
- Core config/setup/routing/verify source docs now describe `config.json` and `.env` under `ARGOS_CONFIG_DIR` instead of hard-coding `~/.argos`.
- Per-task routing docs now describe `routing` config under the Argos config directory's `config.json` instead of hard-coding `~/.argos/config.json`.
- MCP empty-configuration errors now report the actual configured `mcp.json` path instead of hard-coded `~/.argos/mcp.json`.
- MCP/LSP/hooks source docs now describe external-surface config under `ARGOS_CONFIG_DIR/{mcp,lsp,hooks}.json` instead of hard-coding `~/.argos`.
- TUI command source docs now describe hooks, LSP, permissions, and MCP command config files under `ARGOS_CONFIG_DIR` instead of hard-coding `~/.argos`.
- Runtime source docs now describe workspace, verify, and MCP defaults under `ARGOS_CONFIG_DIR/{workspace,verify,mcp.json}` instead of hard-coding `~/.argos`.
- Learning, vision, and skills source docs now describe caches, candidates, locks, and installed skills under `ARGOS_CONFIG_DIR` instead of hard-coding `~/.argos`.
- Persistence source docs now describe snapshots, ledger journals, the SQLite memory DB, daemon index/pid state, and worktree paths under `ARGOS_CONFIG_DIR` instead of hard-coded `~/.argos`.
- Skills curator docs now describe installed skills under the Argos config directory's `skills/` root instead of hard-coding `~/.argos/skills`.
- Voice/image input docs now describe planned STT config under the Argos config directory instead of hard-coding `~/.argos/config.json` and `~/.argos/.env`.
- Dream docs now describe report and conductor order paths under the Argos config directory instead of hard-coding `~/.argos/dreams` and `~/.argos/conductor`.
- Dream/conductor source docstrings now describe report and order paths under `ARGOS_CONFIG_DIR/dreams` and `ARGOS_CONFIG_DIR/conductor` instead of hard-coding `~/.argos`.
- Multirun docs now describe daemon worktree paths under the Argos config directory instead of hard-coding `~/.argos/worktrees`.
- TUI `/dream` inline-mode refusal now points at the active `ARGOS_CONFIG_DIR/daemon.sock` path instead of hard-coded `~/.argos/daemon.sock`.
- Remaining runtime locale strings for external surface warnings and egress denial guidance now show active configured paths instead of hard-coded `~/.argos/...` paths; `argos/locales` no longer contains `~/.argos`.
- Seatbelt sandbox profiles now deny reads of active `ARGOS_CONFIG_DIR/{.env,config.json,mcp.json}` files while preserving workspace readability under the config root.
- Linux bwrap sandbox argv now masks raw and resolved active `ARGOS_CONFIG_DIR/{.env,config.json,mcp.json}` files with `/dev/null`, matching the Seatbelt active-config secret protection even when the config root is symlinked.
- TUI multirun tab activation now treats non-200 focus responses as failures, keeping the previous active run instead of pretending the tab switch succeeded.
- SECURITY now describes hooks under the Argos config directory, names both macOS Seatbelt and Linux bwrap sandboxing, and states the current release has no published binary assets yet.
- Release installer messaging now points non-macOS-arm64 users at stage #13 packaging channels instead of the stale #12 wording, and the Linux `.deb` fallback recommends the Linux Homebrew formula instead of the macOS cask.
- macOS and Linux one-line installers now fail closed when the latest release lacks `SHA256SUMS`, when checksum download fails, or when the checksum entry for the selected asset is missing.
- `argospkg manifest` now fail-closes when WinGet manifests still contain placeholder values, so release review cannot look ready while `InstallerSha256` is still unresolved.
- Homebrew tap bump workflow now defines its `HAS_TOKEN` gate from `secrets.HOMEBREW_TAP_TOKEN`, so the live checkout/update/commit path can run when the release secret is configured.
- GitHub release workflow now generates `SHA256SUMS` from files inside downloaded artifact subdirectories instead of hashing top-level directories and swallowing failures.
- GitHub release workflow now fails before checksum generation when no downloaded release artifacts exist, preventing an empty-artifact release that only contains `SHA256SUMS`.
- GitHub release workflow now fails before `gh release create` when the selected release asset list contains only checksum files, preventing checksum-only releases.
- GitHub release build jobs now set `if-no-files-found: error` for uploaded OS artifacts, so missing `dist/` outputs fail in the build job instead of surfacing later as a partial release.
- WinGet manifest bump workflow now fails when the Windows zip digest cannot be found, instead of committing a half-updated manifest.
- WinGet manifest bump workflow now fails when any of its three manifest files is missing, instead of silently skipping missing files and committing a partial update.
- Homebrew and WinGet bump workflows now require an explicit `tag` input for manual `workflow_dispatch` runs and reuse the release/manual tag expression in update and commit messages.
- Homebrew tap bump workflow now fails when either the Linux AppImage digest or macOS arm64 tarball digest is missing, instead of committing tap files with placeholder SHA values.
- Homebrew tap bump workflow now replaces existing `sha256` values as well as placeholders, so later releases do not keep stale tap checksums from the previous version.
- Shell hard rules now deny additional destructive `rm -rf` variants including root globs, `--` separators, quoted `$HOME`, and `${HOME}` before Trust Dial or broker approval can auto-run them.
- Linux packaging now treats AppImage as a release-blocking artifact: missing `appimagetool`, AppImage build failure, or a missing AppImage file exits non-zero instead of continuing with partial Linux artifacts.
- Linux packaging now also treats `.deb` and `.rpm` as release-blocking assets: missing `dpkg-deb`, missing `rpmbuild`, failed rpm builds, missing rpm output, or failed rpm renaming exit non-zero before artifact upload.
- `argospkg manifest` now lists all three WinGet manifest files and validates `InstallerSha256` as a 64-character hex digest, so non-placeholder garbage hashes cannot pass release review.
- Malformed `permissions.json` now fails closed into `observe` mode on first lazy load instead of silently becoming an empty policy that can auto-approve commands.
- Malformed `hooks.json` now fails closed on first lazy load instead of silently disabling all hooks, while a missing hooks file still means no hooks.
- PreToolUse hook timeouts now block tool execution like other failed PreToolUse hooks.
- Sync-bridge `run_command` now fails closed when the OS sandbox is disabled, preventing subagent/headless fallback paths from executing host shell commands without interactive approval or kernel isolation.
- No-host-loop sync bridge now denies all `browser_*` actions, `browser_navigate` runs through the SSRF/egress preflight, and `browser_screenshot` paths are confined to the active workspace before browser execution.
- `argospkg manifest` now requires all three WinGet manifest files before reporting ready.
- Linux `.deb` missing-asset fallback now points users to the current source-install path instead of unpublished PyPI/Homebrew channels.
- Homebrew tap mirror files are marked draft/template-only until release assets and checksums exist.
- README and setup wizard docs now describe `~/.argos` as the default Argos config directory and document `ARGOS_CONFIG_DIR` as the override for setup, routing, MCP/LSP/hooks/permissions, Dream orders, and uninstall/reset guidance.
- Context visualization docs now describe `compact_threshold` under the Argos config directory's `config.json` instead of hard-coding `~/.argos/config.json`.
- Current Python release artifacts target `0.1.1` after deleting the old remote `v0.1.0` release/tag; local builds produce the `argos-agent` wheel/sdist, expose the `argos` and `argospkg` console scripts, and report `argos 0.1.1`.
- Packaging helper entry points are verified from both the working tree and the built wheel.
- Current broad verification after browser broker hardening passed with `uv run pytest -m "not slow" --no-cov`: `4877 passed, 16 skipped, 37 deselected`.
- Runtime sandbox defaults and legacy file-tool helpers now resolve workspace/verify directories from `ARGOS_CONFIG_DIR` at runtime while preserving explicit env vars and test-injected module overrides.
- Eval corpus defaults now load task manifests from `ARGOS_CONFIG_DIR/eval/corpus` while preserving `ARGOS_EVAL_CORPUS_DIR` as the explicit override.
- Eval user docs now describe reports, corpus, runs, and worktrees under the Argos config directory's `eval/` root instead of hard-coding `~/.argos/eval`.
- Eval source docstrings now describe corpus, result, report, runner, and CLI eval roots under `ARGOS_CONFIG_DIR/eval` instead of hard-coding `~/.argos/eval`.
- Embedding cache defaults now write `embeddings.json` under `ARGOS_CONFIG_DIR` while preserving `ARGOS_EMB_CACHE` and test-injected cache paths.
- Workflow subagent diff journals now write full diff references under `ARGOS_CONFIG_DIR/workflow/diffs` instead of hard-coded `~/.argos/workflow/diffs`.
- Auto-memory JSONL tiers and global instruction lookup now default under `ARGOS_CONFIG_DIR` while preserving `ARGOS_MEMORY_DIR` and `ARGOS_HOME` as explicit overrides.
- Auto-memory docs now recommend `AGENTS.md` for project instructions, document `CLAUDE.md` as compatibility support, and describe memory/global-instruction paths under the Argos config directory instead of hard-coded `~/.argos` paths.
- Terminal-Bench eval CLI runs now use the shared `ARGOS_CONFIG_DIR/eval` base instead of hard-coded `~/.argos/eval`.
- Daemon Dream report override paths now expand `ARGOS_DREAMS_DIR=~/...` the same way the CLI Dream path does.
- Auto-memory explicit override paths now expand `ARGOS_MEMORY_DIR=~/...` and `ARGOS_HOME=~/...` before use.
- Eval corpus explicit override paths now expand `ARGOS_EVAL_CORPUS_DIR=~/...` before use.
- Legacy memory migration paths now expand `ARGOS_MEMORY_FILE=~/...` before importing old JSONL memories.
- Memory database override paths now expand `ARGOS_DB_PATH=~/...` before SQLite opens the database.
- New daemon/app runs now read the latest module-level permissions config from `get_config()` when `build_run_stack()` is called, so `/permissions reload` applies to future runs without restarting Argos.
- GitHub release creation now uses an exact `release-assets.txt` manifest and builds `SHA256SUMS` from that manifest only, preventing same-extension staging files from being published or checksummed.
- TUI `UserPromptSubmit` hook failures now emit visible `HookFired` activity rows while preserving the existing non-blocking run-start behavior.
- Daemon-created runs now persist the owner TUI session id separately from `run_id`, and `RunWorker` passes that stable session id into `AgentLoop.run()` so daemon mode preserves multi-turn model context across sequential prompts.
- Public install docs no longer depend on Python package-index publishing; the launch path is curl/source-first.
- `argospkg info` no longer reads `packaging/VERSION` from the caller's current directory, preventing installed tools from reporting another project's version file.
- Windows release packaging now uses PowerShell `Compress-Archive` for zip assets instead of relying on a bare `zip` CLI on `windows-latest`.
- WinGet locale metadata no longer claims unpublished macOS, Homebrew, curl, Linux, AppImage, `.deb`, or `.rpm` channels are already installable.
- WinGet multi-file manifests now match the official 1.6 schema roles: the root file is a `version` manifest with `DefaultLocale`, the `locale.en-US` file is the `defaultLocale` manifest, and installer `UpgradeBehavior` is a schema-valid scalar.
- macOS arm64 release packaging now strips a leading `v` from `ARGOS_VERSION`, writes that release version into the app bundle `Info.plist`, and leaves the source `packaging/Info.plist` template unchanged.
- Public launch install scope is now versionless `curl | bash` and source checkout; binary installers and package-manager channels are deferred backlog and `release.yml` is manual-only.
- Root `install.sh` now provides a Hermes-style `curl | bash` entrypoint that installs from a GitHub source ref through `uv tool install --force`; it does not add another binary/package-manager channel.
- Root `install.sh` now bootstraps `uv` through Astral's official installer when `uv` is missing, then continues the same `uv tool` install path.
- Root `install.sh` verifies the installed `argos` command through `uv tool dir --bin`, so first-time installs do not fail merely because the uv tool bin directory is not yet on `PATH`.
- Deferred binary installers under `packaging/install.sh` and `packaging/install-deb.sh` now say they are not the public launch installer and point users back to the release/source-checkout launch path.
- Python package-index publishing is not part of the current launch path.
- README and SECURITY now avoid over-claiming that broker governance OS-contains raw CodeAct Python; public security wording limits broker guarantees to declared privileged tools and names `--sandbox` as the opt-in kernel backstop.
- TUI transcript history can now be reviewed without losing the input workflow: `PageUp` / `PageDown` and bubbled mouse-wheel events are routed to the transcript scroll container, and clicking the main transcript gives it focus for history review while startup still defaults focus to the prompt.
- System prompt tool routing now sends external or real-time information requests to `web_search` / `web_extract` / browser tools before Python or shell escape hatches, and explicitly warns that CodeAct timeouts are not proof of sandbox/network failure.
- CodeAct execution now requires an explicit ` ```python` fenced block; ordinary Markdown fences in prose are ignored instead of being executed as Python.
- CodeAct now treats assistant replies that ask the user for confirmation before a ` ```python` block as a waiting conversational turn, so `web_search` / `web_extract` or other tools are not executed after the model says it is waiting for approval.
- CodeAct now nudges research promises such as "look it up" or "fetch the README" into real tool use instead of ending the turn as ordinary conversation.

## Active Task

- `/goal` product ship-readiness continues on branch `codex/curl-only-release-surface`. Current focus is the curl/source launch path, public-doc pruning, and keeping PyPI/binary/package-manager channels out of the public launch surface until there is real user demand.

## Recent Changes

- Added the project `Codex Project Workflow` section to `AGENTS.md`.
- Updated the user-level Codex workflow rules and the `dynamic-workflow` skill to align natural-language tasks, `/goal`, subagents, worktrees, PRs, and conflict handling.
- Updated project memory files to record the workflow configuration change.
- Fixed focused-prompt TUI transcript paging, mouse-wheel fallback scrolling, and click-to-focus transcript review, with regression tests for all three paths.
- Fixed the prompt routing bug that let weather/latest-fact tasks drift into ad-hoc `curl` inside CodeAct and then misreport timeouts as sandbox network failures.
- Fixed the follow-up sandbox/web wording drift in runtime context, CLI `--sandbox` help, and README sandbox docs so they distinguish CodeAct child direct-network confinement from host-side broker web tools.
- Fixed CodeAct extraction so creative/prose responses that include ordinary Markdown log blocks do not spawn a `python · step` action.
- Fixed CodeAct execution so a model response that asks for user permission before an explicit ` ```python` block does not run the block in the same turn.
- Fixed CodeAct no-action handling so research promises such as "look up the repo / fetch the README" do not require repeated user prompts before the agent acts.
- Tightened README/SECURITY security wording so default-off OS sandbox behavior is not oversold as broker-level OS containment for raw model-authored Python.
- Fixed TUI ActivityPanel cache metrics so cache-hit and cache sparkline values use the shared token abbreviation and show the `tok` unit.
- Fixed TUI context pressure so providers that omit usage get an estimated `context_used`, and the status bar refreshes from the same active model window used by the ActivityPanel.
- Deleted the old GitHub `v0.1.0` release and remote tag, then moved source/package metadata and public docs to the fresh `v0.1.1` launch target.
- Collapsed `CHANGELOG.md` to the current unpublished release notes only, removing old `0.1.0` / pre-release history so Argos is treated as not formally released yet.
- Removed Chinese from code comments and docstrings across source, tests, scripts, workflow/config, and packaging templates; Chinese user-facing strings and documentation prose remain allowed where intentional.
- Repaired the English source-doc contracts after comment cleanup so README drift tests still verify `ARGOS_CONFIG_DIR` paths, TUI compare syntax, and TUI glyph provenance without reintroducing Chinese docstrings.
- Root `install.sh` now defaults to installing from the GitHub `main` source ref through `uv tool install --force`; users can still pin a tag or commit with `ARGOS_INSTALL_REF`.
- PyPI/TestPyPI publishing workflow has been removed from the launch path; local wheel/sdist packaging remains available for verification.
- Public docs no longer ship the internal `docs/superpowers/` design/plan archive, and public doc links to that archive were removed.
- Public docs no longer ship the internal acceptance checklist, historical product-definition proof doc, or v6 architecture design archive.
- MCP client initialization now reports the current package version instead of a hard-coded stale version.
- Runtime callers now share the public `config.config_dir()` helper for Argos config-root defaults across CLI, TUI, daemon, tools, eval, memory, skills, ledger, and setup paths; sandbox backend paths that intentionally depend on a supplied sandbox home keep their local logic.
- SQL/Nix/CSS and embedded code comments are covered by an English-only source comment regression test.

## Known Issues

- The worktree already contains broad pre-existing uncommitted changes. Treat them as user-owned unless explicitly told otherwise.
- Current branch `codex/ship-readiness` inherited broad pre-existing uncommitted changes across project memory/instructions; keep future edits narrowly scoped and do not revert unrelated changes.
- Next product closeout targets: review the remaining instruction/memory diffs before PR/release handoff; avoid expanding into unsigned binary/channel work unless explicitly preparing a release.
- `CLAUDE.md` and `AGENTS.md.bak` are intentionally absent by user decision; do not recreate them.
- `argos setup status` may report `.env.local` as a development fallback key source when no `config.json` exists; this reports only the source label, not the key value.
- The old GitHub `v0.1.0` release and remote `v0.1.0` tag were deleted after explicit user confirmation because the release had no installable asset and could mislead launch users.
- Code comments and docstrings should stay English-only; Chinese is acceptable for user-facing locale strings, tests that intentionally exercise Chinese input, and Chinese documentation prose.

## Important Context

- Project instructions live in `AGENTS.md`; keep that file intact and read it before code edits.
- For non-trivial coding work, read project memory files first and update only the ones that are meaningful for the completed change.
- Targeted tests should usually use `uv run pytest <path> -q --no-cov`.
- Smoke check for runtime behavior: `uv run argos --selftest`.

## Next Step

- Keep future changes small, verified, and aligned with existing `uv` workflows.
- Before coding, inspect the relevant existing code path and add the narrowest useful verification.
- Pick one bounded product or bug target before editing; avoid opportunistic cleanup while the large uncommitted branch is in progress.
- Before PR/release handoff, verify the raw GitHub curl installer path, README/docs links, and targeted packaging/readme/MCP tests. Do not reintroduce PyPI as a launch blocker.

## Verification Notes

- Latest curl/source launch and config-root helper verification: `uv run pytest tests/test_install_script.py tests/test_packaging_pypi.py tests/test_readme_docs.py tests/test_mcp_native.py tests/test_packaging_release_workflow.py tests/test_config_loader.py -q --no-cov` passed (`168 passed, 1 skipped, 2 deselected`); `uv run pytest -m "not slow" --no-cov` passed (`4921 passed, 16 skipped, 37 deselected`); `uv run argos --selftest` passed; `bash -n install.sh packaging/install.sh packaging/install-deb.sh` passed; `git diff --check` passed; code comment/docstring Chinese scanner returned `0`.
- Latest public-doc/comment cleanup verification: `uv run pytest tests/test_readme_docs.py tests/test_install_script.py tests/test_packaging_pypi.py tests/test_mcp_native.py tests/test_config_loader.py -q --no-cov` passed (`155 passed, 1 skipped, 2 deselected`); `uv run pytest -m "not slow" --no-cov` passed (`4919 passed, 16 skipped, 37 deselected`); `uv run argos --selftest` passed; `python -m compileall -q argos scripts packaging smoke_packaged.py` passed; `bash -n install.sh packaging/install.sh packaging/install-deb.sh packaging/build_arm64.sh packaging/build_linux.sh packaging/build_windows.sh` passed; `git diff --check` passed.
- Latest PR handoff: PR #34 (`https://github.com/tungoldshou/argos/pull/34`) is ready for review from `codex/curl-only-release-surface` to `main`; its GitHub `Full test suite (Linux + bwrap sandbox)` check passed in 4m18s on commit `6f15516`.
- Latest TUI scroll verification: `uv run pytest tests/test_tui_wiring.py -q --no-cov` (`26 passed`), `uv run pytest tests/tui/test_tuiapp_lane.py -q --no-cov` (`49 passed`), and `uv run argos --selftest`.
- Latest prompt-routing verification: red test `tests/test_prompt_tool_exposure.py::test_external_info_is_routed_before_shell_escape_hatches` failed before the fix; after the fix, `uv run pytest tests/test_prompt_tool_exposure.py -q --no-cov` passed (`9 passed`), `uv run pytest tests/test_loop_codeact.py tests/test_loop_user_goal.py tests/test_prompt_tool_exposure.py -q --no-cov` passed (`37 passed`), and `uv run argos --selftest` passed.
- Latest CodeAct fence verification: red tests caught ordinary Markdown fences being extracted and executed as Python; after the fix, `uv run pytest tests/test_loop_codeact.py::test_extract_code_block tests/test_loop_codeact.py::test_loop_ignores_non_python_markdown_fences -q --no-cov` passed (`2 passed`), `uv run pytest tests/test_loop_codeact.py -q --no-cov` passed (`25 passed`), and transcript/prompt routing checks passed with `23 passed`.
- Latest ActivityPanel cache metric verification: red test caught raw cache values (`13568`) in cache-hit and sparkline lines; after the fix, `uv run pytest tests/test_activity_panel.py tests/test_tui_fmt.py tests/tui/test_activity_panel_audit.py::TestCacheSparklineCyan -q --no-cov` passed (`48 passed`), and `uv run argos --selftest` passed.
- Latest TUI context status verification: red tests caught missing provider usage leaving `context_used=0` and `CostUpdate.context_used` not updating `StatusBar.ctx_pct`; after the fix, `uv run pytest tests/test_loop_codeact.py tests/test_tui_context.py tests/test_activity_panel.py tests/test_protocols.py::test_anthropic_context_total_is_input_plus_cache tests/test_protocols.py::test_openai_context_total_does_not_double_count_cache -q --no-cov` passed (`81 passed`).
- Latest sandbox/web wording verification: red tests for runtime context, top-level CLI help, and README sandbox docs failed before the fix; after the fix, `uv run pytest tests/test_prompt_tool_exposure.py tests/test_honesty_prompt_sections.py tests/test_cli_setup.py tests/test_readme_docs.py -q --no-cov` passed (`95 passed`), `uv run argos --selftest` passed, and `git diff --check` passed for the touched files.
- Latest workflow-config verification: inspected project/user workflow files and ran targeted `rg` checks for old required prefixes and unsafe merge/force-push rules; no business-code tests were needed because this change only updates Codex workflow documentation/configuration.
- Latest closeout verification: `uv run pytest tests/test_mcp_native.py tests/test_external_surfaces.py tests/test_prompt_tool_exposure.py tests/test_skills.py tests/test_skills_curator_index.py tests/learning/test_candidates.py tests/test_audit_log.py tests/test_cli_setup.py tests/test_cli_dream.py tests/conductor/test_daemon_wiring.py::test_material_gate_silences_empty_candidates tests/conductor/test_dream_autonomous.py -q --no-cov` (`128 passed`) and `uv run argos --selftest`.
- Latest ship-readiness verification: `uv run pytest tests/test_daemon_spawn_args.py tests/test_daemon_version_handshake.py tests/daemon/test_daemon_cli.py tests/test_tui_daemon_client_mode.py -q --no-cov` (`42 passed`), `uv run argos --selftest`, and `uv run pytest -m "not slow" --no-cov` (`4754 passed, 15 skipped, 37 deselected`).
- Latest TUI path-hint verification: `uv run pytest tests/test_tui_commands.py tests/test_tui_hooks_cmd.py tests/test_tui_lsp_cmd.py tests/test_tui_capability_commands.py -q --no-cov` (`89 passed`), `uv run argos --selftest`, and `git diff --check`.
- Latest CLI setup path-hint verification: red tests for setup help, paste-key warning, and non-TTY fallback; `uv run pytest tests/test_cli_setup.py tests/test_setup_wizard.py -q --no-cov` (`97 passed`), `uv run argos --selftest`, and `git diff --check`.
- Latest MCP empty-config verification: red test for `McpManager.call()` no-server errors; `uv run pytest tests/test_mcp_native.py tests/test_external_surfaces.py tests/test_prompt_tool_exposure.py tests/test_tui_commands.py::test_mcp_empty_mentions_configured_path -q --no-cov` (`25 passed`), `uv run argos --selftest`, and `git diff --check`.
- Latest Dream daemon-hint verification: red test for `/dream` inline refusal; `uv run pytest tests/test_cli_dream.py tests/test_tui_daemon_client_mode.py -q --no-cov` (`28 passed`), `uv run argos --selftest`, and `git diff --check`.
- Latest runtime locale path verification: red tests for external surface warnings and egress denial guidance; `rg -n -F -- "~/.argos" argos/locales` returned no matches; targeted locale/config path tests passed (`157 passed`); `uv run pytest -m "not slow" --no-cov` (`4763 passed, 15 skipped, 37 deselected`); `uv run argos --selftest`; `git diff --check`.
- Latest docs path verification: red test requiring `ARGOS_CONFIG_DIR` in setup docs; `uv run pytest tests/test_readme_docs.py -q --no-cov` (`20 passed`), `uv run argos --selftest`, and `git diff --check`.
- Latest Python packaging verification: `uv build` produced `dist/argos_agent-0.1.0.tar.gz` and `dist/argos_agent-0.1.0-py3-none-any.whl`; a clean temporary venv installed the wheel and `argos --version` returned `argos 0.1.0`; `argos --selftest` returned OK.
- Earlier packaging helper verification passed before the WinGet placeholder gate was tightened. Current release readiness evidence supersedes it: `argospkg manifest` must return non-zero until the real WinGet `InstallerSha256` is written from release assets.
- Latest PyPI/release-local verification: red tests caught `argospkg info` reading CWD `packaging/VERSION`, README PyPI install text hard-coding unpublished/404 status, and Windows packaging relying on bare `zip`; after fixes, `uv run pytest tests/test_readme_docs.py tests/test_packaging_pypi.py tests/test_packaging_release_workflow.py tests/test_packaging_windows_spec.py -q --no-cov` passed (`86 passed, 1 deselected`), `uv build` passed, installed-wheel `argospkg info` from a fake CWD did not read `packaging/VERSION: 9.9.9`, `bash -n packaging/build_windows.sh` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest WinGet metadata verification: red test caught the locale description claiming unpublished non-Windows channels were live; after the fix, `uv run pytest tests/test_packaging_winget.py tests/test_packaging_pypi.py::test_argospkg_manifest_fails_on_placeholder_winget_digest tests/test_readme_docs.py::test_readme_pypi_install_text_is_release_neutral -q --no-cov` passed (`11 passed`), `uv run argospkg manifest` still returned 1 on the required WinGet placeholder digest, GitHub `v0.1.0` still had only `SHA256SUMS`, PyPI/TestPyPI still returned HTTP 404, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest A1 setup smoke: with a temporary `ARGOS_CONFIG_DIR`, `uv run argos setup status`, `uv run argos setup --help`, and `uv run argos --selftest` all exited 0.
- Latest collaboration-doc correction: removed the recreated `CLAUDE.md` file after the user confirmed `CLAUDE.md` and `AGENTS.md.bak` were intentionally deleted.
- Latest setup docs verification: red test for missing `.env.local` status fallback documentation, then `uv run pytest tests/test_readme_docs.py tests/test_cli_setup.py::test_setup_status_labels_env_local_fallback tests/test_cli_setup.py::test_setup_status_reports_env_fallback_without_config -q --no-cov` passed (`23 passed`).
- Latest ActivityPanel MCP regression: `uv run pytest tests/test_tui_activity_panel.py tests/test_mcp_native.py::test_default_path_honors_argos_config_dir tests/test_tui_commands.py::test_mcp_empty_mentions_configured_path -q --no-cov` passed (`8 passed`).
- Latest broad pre-handoff verification: `uv run pytest -m "not slow" --no-cov` passed (`4765 passed, 15 skipped, 37 deselected`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest permission hard-rule verification: red test for `ARGOS_CONFIG_DIR/.env` not being recognized as Argos' own env file, then `uv run pytest tests/test_hard_path_rules.py tests/test_broker_request.py::test_egress_deny_reason_message_format -q --no-cov` passed (`19 passed`).
- Latest self-update cache verification: red tests for `ARGOS_CONFIG_DIR/.last_update_check`, then `uv run pytest tests/test_argos_self_update_cmd.py tests/test_self_update.py -q --no-cov` passed (`14 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest memory store verification: red test for default `ArgosStore()` ignoring `ARGOS_CONFIG_DIR`, then `uv run pytest tests/test_store_schema.py -q --no-cov` passed (`8 passed`), selected `tests/test_app_factory.py` component tests passed (`3 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest ledger store verification: red test for default `LedgerStore()` ignoring `ARGOS_CONFIG_DIR`, then `uv run pytest tests/ledger/test_store_file_undo.py tests/ledger/test_undo_flow.py -q --no-cov` passed (`17 passed`), `uv run pytest tests/ledger/test_inline_ledger.py -q --no-cov` passed (`11 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest isolation root verification: red tests for sandbox/worktree roots ignoring `ARGOS_CONFIG_DIR`, then `uv run pytest tests/test_isolation.py -q --no-cov` passed (`10 passed`), `uv run pytest tests/test_daemon_multirun.py::test_post_runs_with_isolation_creates_worktree -q --no-cov` passed, `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest app workspace verification: red test for `build_components()` default workspace ignoring `ARGOS_CONFIG_DIR`, then `uv run pytest tests/test_app_factory.py -q --no-cov` passed (`10 passed`), selected CLI setup smoke tests passed (`2 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest AgentLoop default-dir verification: red test for direct `AgentLoop(...)` defaults ignoring `ARGOS_CONFIG_DIR`, then `uv run pytest tests/test_loop_user_goal.py -q --no-cov` passed (`4 passed`), `uv run pytest tests/test_loop_runtime_context.py tests/test_loop_multiturn.py -q --no-cov` passed (`5 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest runtime/tool default-dir verification: red tests for `runtime.use_sandbox()` and file-tool defaults ignoring `ARGOS_CONFIG_DIR`, then `uv run pytest tests/test_app_factory.py tests/test_runtime.py tests/test_tools_files.py tests/test_tools_namespace.py tests/test_verify_gate_fixes.py tests/test_loop_runtime_context.py -q --no-cov` passed (`67 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest eval corpus verification: red test for default corpus root ignoring `ARGOS_CONFIG_DIR`, then `uv run pytest tests/test_eval_corpus.py tests/test_eval_cli.py tests/test_eval_e2e.py tests/test_eval_runner.py tests/test_eval_compare.py tests/test_eval_tui.py -q --no-cov` passed (`77 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest embedding cache verification: red test for default cache path lacking `ARGOS_CONFIG_DIR` support, then `uv run pytest tests/test_llm_embed.py tests/test_embedding.py tests/test_store_schema.py tests/test_skills.py -q --no-cov` passed (`32 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest workflow diff journal verification: red test for `_persist_diff_journal()` writing under real `~/.argos/workflow/diffs`, then `uv run pytest tests/workflow/test_subagent_diff_summary.py -q --no-cov` passed (`7 passed`), `uv run pytest tests/workflow/test_subagent.py tests/workflow/test_best_of_n.py tests/workflow/test_engine_ops.py -q --no-cov` passed (`21 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest auto-memory root verification: red tests for `_root()` and `_ARGOS_HOME()` ignoring `ARGOS_CONFIG_DIR`, then `uv run pytest tests/test_memory_tiers.py tests/test_memory_injection.py tests/test_memory_capture.py tests/test_memory_commands.py tests/test_memory_ranking.py tests/test_memory_decay.py tests/test_loop_user_goal.py -q --no-cov` passed (`82 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest Terminal-Bench eval root verification: red test for `cmd_tb()` creating its EvalRunner base under real `~/.argos/eval`, then `uv run pytest tests/eval/test_terminal_bench_sync_output.py tests/eval/test_terminal_bench_adapter.py tests/eval/test_terminal_bench_best_of_n.py tests/eval/test_terminal_bench_verify_whitelist.py -q --no-cov` passed (`38 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest daemon Dream override verification: red test for `ARGOS_DREAMS_DIR=~/...` returning a literal relative path in daemon server, then `uv run pytest tests/daemon/test_dream_autonomous_server_guards.py tests/test_cli_dream.py tests/conductor/test_daemon_wiring.py::test_material_gate_silences_empty_candidates tests/conductor/test_dream_autonomous.py -q --no-cov` passed (`34 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest auto-memory explicit override verification: red tests for `ARGOS_MEMORY_DIR=~/...` and `ARGOS_HOME=~/...` returning literal relative paths, then `uv run pytest tests/test_memory_tiers.py tests/test_memory_injection.py tests/test_memory_capture.py tests/test_memory_commands.py tests/test_memory_ranking.py tests/test_memory_decay.py tests/test_loop_user_goal.py -q --no-cov` passed (`84 passed`), `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest eval corpus override verification: red test for `ARGOS_EVAL_CORPUS_DIR=~/...` returning an empty corpus from a literal relative path, then `uv run pytest tests/test_eval_corpus.py tests/test_eval_cli.py tests/test_eval_e2e.py tests/test_eval_runner.py tests/test_eval_compare.py tests/test_eval_tui.py -q --no-cov` passed (`78 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest legacy memory migration override verification: red test for `ARGOS_MEMORY_FILE=~/...` returning zero migrated rows from a literal relative path, then `uv run pytest tests/test_store_migrate.py tests/test_store_schema.py tests/test_memory_tiers.py tests/test_memory_injection.py tests/test_memory_capture.py tests/test_memory_commands.py tests/test_memory_ranking.py tests/test_memory_decay.py -q --no-cov` passed (`96 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest memory database override verification: red test for `ARGOS_DB_PATH=~/...` creating no database under `HOME`, then `uv run pytest tests/test_store_schema.py tests/test_store_migrate.py tests/test_memory_tiers.py tests/test_memory_injection.py tests/test_memory_capture.py tests/test_memory_commands.py tests/test_memory_ranking.py tests/test_memory_decay.py -q --no-cov` passed (`97 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest broad ship-readiness verification: `uv run pytest -m "not slow" --no-cov` passed (`4793 passed, 15 skipped, 37 deselected`), `uv run argos --selftest` passed, `git diff --check` passed, `uv build` produced `dist/argos_agent-0.1.0.tar.gz` and `dist/argos_agent-0.1.0-py3-none-any.whl`, and a clean temporary venv installed the wheel, reported `argos 0.1.0`, and passed installed-wheel `argos --selftest`.
- Latest repo hygiene verification: `git check-ignore -v --no-index CLAUDE.md .learnings/ERRORS.md AGENTS.md` confirms `CLAUDE.md` and `.learnings/` are ignored while root `AGENTS.md` remains unignored; `git ls-files -o --exclude-standard` returned no untracked files; `CLAUDE.md` remains deleted and `AGENTS.md.bak` remains absent.
- Latest docs drift verification: red test caught README hard-coding `~/.argos/memory/...`; README now describes memory paths under the Argos config directory, CONTRIBUTING now cites `AGENTS.md` instead of deleted `CLAUDE.md`, and `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`23 passed`).
- Latest auto-memory docs verification: red test caught `docs/auto-memory.md` recommending `CLAUDE.md` and hard-coded config paths; docs now recommend `AGENTS.md`, keep `CLAUDE.md` as compatibility, and `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`24 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest daemon socket docs verification: red test caught `CONTRIBUTING.md` and `docs/argos-product-definition.md` hard-coding `~/.argos/daemon.sock`; docs now describe `daemon.sock` under the Argos config directory, and `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`25 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest eval docs verification: red test caught `docs/eval.md` hard-coding `~/.argos/eval`; docs now use `ARGOS_CONFIG_DIR` / `<config-dir>/eval`, and `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`26 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest routing docs verification: red test caught `docs/per-task-routing.md` hard-coding `~/.argos/config.json`; docs now use `ARGOS_CONFIG_DIR` / Argos config directory wording, and `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`27 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest context docs verification: red test caught `docs/context-viz.md` hard-coding `~/.argos/config.json`; docs now use `ARGOS_CONFIG_DIR` / Argos config directory wording, and `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`28 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest acceptance checklist verification: red test caught `docs/acceptance-checklist.md` hard-coding `~/.argos/mcp.json`; docs now use Argos config directory / `ARGOS_CONFIG_DIR` wording, and `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`29 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest skills curator docs verification: red test caught `docs/skills-curator.md` hard-coding `~/.argos/skills`; docs now use Argos config directory / `ARGOS_CONFIG_DIR` wording, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`30 passed`), `uv run pytest tests/test_skills_curator_index.py::test_default_skills_root_honors_argos_config_dir -q --no-cov` passed, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest voice/image docs verification: red test caught `docs/voice-image-input.md` hard-coding `~/.argos/config.json` and `~/.argos/.env`; docs now use Argos config directory / `ARGOS_CONFIG_DIR` wording, and `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`31 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest Dream docs verification: red test caught `docs/dream.md` hard-coding `~/.argos/dreams` and `~/.argos/conductor`; docs now use `<config-dir>` / `ARGOS_CONFIG_DIR` wording, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`32 passed`), `uv run pytest tests/test_cli_dream.py::test_cli_dream_default_dirs_honor_argos_config_dir tests/daemon/test_dream_autonomous_server_guards.py::test_dreams_dir_honors_argos_config_dir -q --no-cov` passed (`2 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest multirun docs verification: red test caught `docs/multirun.md` hard-coding `~/.argos/worktrees`; docs now use `<config-dir>` / `ARGOS_CONFIG_DIR` wording, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`33 passed`), `uv run pytest tests/test_daemon_worktree.py::test_default_base_dir_honors_argos_config_dir tests/test_isolation.py::test_default_worktree_root_honors_argos_config_dir -q --no-cov` passed (`2 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest eval source docs verification: red test caught eval source docstrings hard-coding `~/.argos/eval`; source docs now use `ARGOS_CONFIG_DIR/eval`, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`34 passed`), `uv run pytest tests/test_eval_corpus.py tests/test_eval_cli.py tests/test_eval_e2e.py tests/test_eval_runner.py tests/test_eval_compare.py tests/test_eval_tui.py -q --no-cov` passed (`78 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest Dream/conductor source docs verification: red test caught source docstrings lacking `ARGOS_CONFIG_DIR/dreams` and `ARGOS_CONFIG_DIR/conductor`; source docs now use those roots, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`35 passed`), `uv run pytest tests/test_cli_dream.py::test_cli_dream_default_dirs_honor_argos_config_dir tests/daemon/test_dream_autonomous_server_guards.py::test_dreams_dir_honors_argos_config_dir tests/conductor/test_daemon_wiring.py::test_material_gate_silences_empty_candidates tests/conductor/test_dream_autonomous.py -q --no-cov` passed (`9 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest MCP/LSP/hooks source docs verification: red test caught external-surface source docs lacking `ARGOS_CONFIG_DIR/{mcp,lsp,hooks}.json`; source docs now use those roots, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`36 passed`), `uv run pytest tests/test_mcp_native.py tests/test_external_surfaces.py tests/test_prompt_tool_exposure.py tests/test_tui_activity_panel.py tests/test_tui_hooks_cmd.py tests/test_tui_lsp_cmd.py tests/test_tui_commands.py::test_mcp_empty_mentions_configured_path -q --no-cov` passed (`47 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest core config source docs verification: red test caught config/setup/routing/verify source docs lacking `ARGOS_CONFIG_DIR/config.json` and `ARGOS_CONFIG_DIR/.env`; source docs now use those roots, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`37 passed`), `uv run pytest tests/test_cli_setup.py tests/test_setup_wizard.py tests/test_routing_config.py tests/test_routing_e2e.py -q --no-cov` passed (`127 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest persistence source docs verification: red test caught persistence source docs lacking `ARGOS_CONFIG_DIR/{snapshots,ledger,argos.db,runs/index.json,daemon.pid,worktrees}`; source docs now use those roots, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`38 passed`), `uv run pytest tests/test_snapshot_persist.py tests/ledger/test_store_file_undo.py tests/ledger/test_undo_flow.py tests/test_store_schema.py tests/test_store_migrate.py tests/test_isolation.py tests/test_daemon_worktree.py -q --no-cov` passed (`59 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest TUI command source docs verification: red test caught TUI command source docs lacking `ARGOS_CONFIG_DIR/{hooks,lsp,permissions,mcp}.json`; source docs now use those roots, `uv run pytest tests/test_tui_commands.py tests/test_tui_hooks_cmd.py tests/test_tui_lsp_cmd.py tests/test_tui_activity_panel.py tests/test_readme_docs.py -q --no-cov` passed (`130 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest runtime source docs verification: red test caught runtime source docs lacking `ARGOS_CONFIG_DIR/{workspace,verify,mcp.json}`; source docs now use those roots, `uv run pytest tests/test_runtime.py tests/test_loop_user_goal.py tests/test_broker_request.py tests/test_readme_docs.py -q --no-cov` passed (`73 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest learning/skills source docs verification: red test caught learning, vision, and skills source docs lacking `ARGOS_CONFIG_DIR/{vision_cache.json,learning/candidates,learning,skills/index.json,skills}`; source docs now use those roots, `uv run pytest tests/core/test_vision_capability.py tests/test_skills_curator_index.py tests/test_skills_curator_install.py tests/learning/test_hook.py tests/learning/test_dream_pipeline.py tests/learning/test_dream_concurrency.py tests/test_cli_dream.py tests/test_readme_docs.py -q --no-cov` passed (`140 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest repo-wide comment cleanup verification: targeted regression slice for the 14 previous cleanup failures passed (`14 passed`), `uv run pytest -m "not slow" --no-cov` passed (`4907 passed, 16 skipped, 37 deselected`), the code comment/docstring Chinese scanner returned `code_comment_docstring_chinese_findings=0`, `git diff --check` passed, old `v0.1.0` changelog/release references were absent from current launch docs, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest launch URL verification: read-only subagent security review caught the old default-branch raw installer URL would 404 before `install.sh` lands on the default branch; after switching README/docs/tests to `v0.1.1/install.sh`, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`50 passed`), the focused publish/install slice passed (`7 passed`), the broader publish/install/release slice passed (`57 passed, 1 skipped`), `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest remote publish preflight: commit `89fa66b` was pushed to `origin/codex/ship-readiness`; `publish.yml` workflow_dispatch run `28678299904` passed the Linux+bwrap CI gate and the distribution build/twine/wheel-smoke job, skipped formal PyPI as intended for manual dispatch, then failed only at TestPyPI trusted publishing with `invalid-publisher` for the `testpypi` environment claim.
- Latest PR handoff: draft PR #29 (`https://github.com/tungoldshou/argos/pull/29`) is open from `codex/ship-readiness` to `main`; it documents the passed local/remote checks and the remaining TestPyPI trusted-publisher step.
- Latest PR readiness: PR #29 was marked ready for review; GitHub reports `mergeStateStatus=CLEAN` and the PR CI run `28678584666` passed the full Linux+bwrap suite.
- Latest Seatbelt config-secret verification: red test caught sandbox profiles missing active `ARGOS_CONFIG_DIR/{.env,config.json,mcp.json}` read denies; `uv run pytest tests/test_seatbelt_profile.py tests/test_sandbox_fs_confinement.py tests/test_run_command_confined.py -q --no-cov` passed (`17 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest TUI multirun focus verification: red test caught `_on_tab_activated()` switching active tabs on non-200 focus responses; `uv run pytest tests/test_tui_multirun_focus.py -q --no-cov` passed (`7 passed`), `uv run pytest tests/test_tui_commands.py tests/test_tui_daemon_client_mode.py -q --no-cov` passed (`79 passed`), `uv run pytest -m "not slow" --no-cov` passed (`4815 passed, 15 skipped, 37 deselected`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest Linux bwrap active-config masking verification: red tests caught `_bwrap_argv()` missing active `ARGOS_CONFIG_DIR/{.env,config.json,mcp.json}` masks and missing resolved-path masks when `ARGOS_CONFIG_DIR` is symlinked; `uv run pytest tests/sandbox/test_linux_backend.py tests/test_sandbox_fs_confinement.py tests/test_seatbelt_profile.py tests/test_run_command_confined.py tests/test_sandbox_shell_fixes.py -q --no-cov` passed (`39 passed, 4 skipped`), `uv run pytest -m "not slow" --no-cov` passed (`4817 passed, 15 skipped, 37 deselected`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest current-package verification: `uv build` produced `dist/argos_agent-0.1.0.tar.gz` and `dist/argos_agent-0.1.0-py3-none-any.whl`; a clean temporary venv installed the wheel with `uv pip install --python`, `argos --version` returned `argos 0.1.0`, installed-wheel `argos --selftest` returned OK, installed-wheel `argospkg check` returned `import OK`, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest SECURITY drift verification: red tests caught `SECURITY.md` still hard-coding `~/.argos/hooks.json`, omitting Linux bwrap from the sandbox summary, and claiming outdated macOS-only binary status; `uv run pytest tests/test_readme_docs.py::test_security_doc_uses_config_dir_and_current_sandbox_names -q --no-cov` and `uv run pytest tests/test_readme_docs.py::test_security_doc_matches_current_binary_release_status -q --no-cov` failed before their fixes and passed after them, `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`43 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest install-script drift verification: red tests caught `packaging/install.sh` still pointing Linux/Windows users at stale `#12` wording and `packaging/install-deb.sh` recommending the macOS cask as a Linux fallback; `uv run pytest tests/test_install_script.py::test_install_script_mentions_current_packaging_stage -q --no-cov` and `uv run pytest tests/test_packaging_install_scripts.py::test_install_deb_fallback_does_not_recommend_cask_on_linux -q --no-cov` failed before their fixes and passed after them, `uv run pytest tests/test_install_script.py -q --no-cov` passed (`7 passed, 1 deselected`), `uv run pytest tests/test_packaging_install_scripts.py -q --no-cov` passed (`4 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest WinGet manifest gate verification: red test caught `argospkg manifest` returning 0 while `packaging/winget/tungoldshou.argos.installer.yaml` still contained `PLACEHOLDER_FROM_BUMP`; `uv run pytest tests/test_packaging_pypi.py::test_argospkg_manifest_fails_on_placeholder_winget_digest -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_pypi.py -q --no-cov` passed (`16 passed, 1 deselected`), `uv run argos --selftest` passed, `uv run argospkg manifest` returned 1 with a placeholder error, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest Homebrew bump workflow verification: red test caught `.github/workflows/bump-homebrew-formula.yml` using `env.HAS_TOKEN` without defining it; `uv run pytest tests/test_packaging_homebrew.py::test_bump_homebrew_workflow_sets_token_gate_env -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_homebrew.py -q --no-cov` passed (`7 passed`), combined Homebrew/PyPI packaging tests passed (`23 passed, 1 deselected`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest release checksum verification: red test caught `release.yml` using `sha256sum -- * > SHA256SUMS || true` after `download-artifact` creates nested artifact directories; `uv run pytest tests/test_packaging_release_workflow.py::test_release_workflow_hashes_downloaded_artifact_files_recursively -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_release_workflow.py -q --no-cov` passed (`7 passed`), a local shell check generated two checksums from nested artifact files, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest PyPI publish gate verification: red tests caught `publish.yml` ignoring `twine check` failures and not cleaning stale `dist/` before `uv build`; `uv run pytest tests/test_packaging_pypi.py::test_publish_workflow_does_not_ignore_twine_check_failure tests/test_packaging_pypi.py::test_publish_workflow_cleans_dist_before_build -q --no-cov` passed after the fix, `rm -rf dist && uv build && uv run --with twine twine check dist/*` passed, `uv run pytest tests/test_packaging_pypi.py -q --no-cov` passed (`18 passed, 1 deselected`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest WinGet bump digest verification: red test caught `.github/workflows/bump-winget-manifest.yml` warning but continuing when `SHA256_WIN` is empty; `uv run pytest tests/test_packaging_winget.py::test_bump_winget_workflow_fails_when_windows_digest_missing -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_winget.py -q --no-cov` passed (`6 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest bump workflow manual-trigger verification: red tests caught Homebrew and WinGet bump workflows exposing `workflow_dispatch` without a required tag input, leaving `github.event.release.tag_name` empty on manual runs; `uv run pytest tests/test_packaging_homebrew.py::test_bump_homebrew_workflow_dispatch_requires_tag_input tests/test_packaging_winget.py::test_bump_winget_workflow_dispatch_requires_tag_input -q --no-cov` passed after the fix, combined Homebrew/WinGet packaging tests passed (`15 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest Homebrew digest gate verification: red test caught `.github/workflows/bump-homebrew-formula.yml` skipping SHA replacement when release asset digests are missing; `uv run pytest tests/test_packaging_homebrew.py::test_bump_homebrew_workflow_fails_when_release_digests_missing -q --no-cov` passed after the fix, combined Homebrew/WinGet packaging tests passed (`16 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest release empty-artifact gate verification: red test caught `release.yml` lacking a no-artifacts guard before final checksum generation; `uv run pytest tests/test_packaging_release_workflow.py::test_release_workflow_fails_when_no_downloaded_artifacts_exist -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_release_workflow.py -q --no-cov` passed (`8 passed`), local shell checks confirmed empty artifact dirs fail and real nested artifacts still checksum, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest release checksum-only gate verification: red test caught `release.yml` allowing `gh release create` with only `SHA256SUMS` assets; `uv run pytest tests/test_packaging_release_workflow.py::test_release_workflow_fails_when_only_checksum_assets_are_releasable -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_release_workflow.py -q --no-cov` passed (`9 passed`), shell checks confirmed checksum-only assets fail and a real AppImage asset passes the guard, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest release artifact-upload gate verification: red test caught `release.yml` upload-artifact steps missing `if-no-files-found: error`; `uv run pytest tests/test_packaging_release_workflow.py::test_release_workflow_upload_artifacts_fail_when_dist_is_empty -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_release_workflow.py -q --no-cov` passed (`10 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest PyPI artifact-upload gate verification: red test caught `publish.yml` upload-artifact missing `if-no-files-found: error`; `uv run pytest tests/test_packaging_pypi.py::test_publish_workflow_upload_artifact_fails_when_dist_is_empty -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_pypi.py -q --no-cov` passed (`19 passed, 1 deselected`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest WinGet manifest-file gate verification: red test caught `.github/workflows/bump-winget-manifest.yml` continuing when a manifest file was missing; `uv run pytest tests/test_packaging_winget.py::test_bump_winget_workflow_fails_when_manifest_file_missing -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_winget.py -q --no-cov` passed (`8 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest Homebrew stale-sha verification: red test caught `.github/workflows/bump-homebrew-formula.yml` only replacing `PLACEHOLDER_FROM_BUMP` instead of existing real `sha256` values; `uv run pytest tests/test_packaging_homebrew.py::test_bump_homebrew_workflow_replaces_existing_sha_values -q --no-cov` passed after the fix, `uv run pytest tests/test_packaging_homebrew.py -q --no-cov` passed (`10 passed`), a local sed smoke confirmed old real sha values are replaced, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest installer checksum verification: red tests caught `packaging/install.sh` and `packaging/install-deb.sh` warning and continuing when `SHA256SUMS` could not be fetched; `uv run pytest tests/test_install_script.py::test_install_script_requires_sha256sums tests/test_packaging_install_scripts.py::test_install_deb_script_requires_sha256sums -q --no-cov` passed after the fix, `uv run pytest tests/test_install_script.py tests/test_packaging_install_scripts.py -q --no-cov` passed (`12 passed, 1 skipped, 1 deselected`), `bash -n packaging/install.sh packaging/install-deb.sh` passed, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest shell/release gate verification: red tests caught hard-rule misses for `rm -rf /*`, `rm -rf -- /`, quoted `$HOME`, and `${HOME}`; red tests also caught Linux AppImage fail-open wording and non-placeholder invalid WinGet SHA values. After fixes, `uv run pytest tests/test_hard_shell_rules.py tests/test_approval_levels.py::test_cautious_auto_passes_sandboxed_run_command tests/test_broker_request.py::test_dangerous_run_command_still_blocked_at_yolo tests/test_broker_execute_sync.py::test_execute_sync_blocks_dangerous_run_command -q --no-cov` passed (`67 passed`), `uv run pytest tests/test_packaging_linux_spec.py tests/test_packaging_pypi.py -q --no-cov` passed (`27 passed, 1 deselected`), `bash -n packaging/build_linux.sh` passed, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest permissions/hooks fail-closed verification: red tests caught malformed `permissions.json` becoming an empty auto-approving policy, malformed first-load `hooks.json` becoming empty hooks, and PreToolUse timeout still executing `write_file`. After fixes, `uv run pytest tests/test_permissions_config.py tests/test_tui_permissions_cmd.py tests/test_approval_levels.py::test_cautious_auto_passes_sandboxed_run_command tests/permissions/test_l0_l2_semantics.py -q --no-cov` passed (`65 passed`), `uv run pytest tests/test_hooks_config.py tests/test_hooks_runner.py tests/e2e/test_loop_hooks.py tests/test_tui_hooks_cmd.py -q --no-cov` passed (`72 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest sync-bridge/release-handoff verification: red tests caught unsandboxed sync-bridge `run_command`, missing WinGet manifest files, unpublished-channel `.deb` fallback, Homebrew tap README draft drift, legacy Homebrew cask template drift, and README Trust Dial wording drift. After fixes, `uv run pytest tests/test_broker_execute_sync.py tests/test_egress_valve.py::test_execute_sync_keeps_network_off tests/test_packaging_pypi.py tests/test_packaging_homebrew.py tests/test_packaging_install_scripts.py tests/test_readme_docs.py -q --no-cov -m 'not slow'` passed (`91 passed, 1 deselected`), `uv run argos --selftest` passed, `bash -n packaging/install-deb.sh` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest release/security polish verification: red tests caught `packaging-c.md` and the Homebrew tap mirror implying unpublished channels were live, Linux/Windows release scripts preserving tag prefix `v` in artifacts, Cautious mode auto-approving unsandboxed `run_command`, root/home wipe variants with trailing/equivalent slashes, and `/permissions reload` not updating the current gate config. After fixes, `uv run pytest tests/test_hard_shell_rules.py tests/test_egress_valve.py tests/test_approval_levels.py::test_cautious_auto_passes_sandboxed_run_command tests/test_broker_request.py::test_run_command_auto_runs_at_yolo tests/test_permissions_config.py tests/test_tui_permissions_cmd.py tests/test_packaging_linux_spec.py tests/test_packaging_windows_spec.py tests/test_packaging_release_workflow.py tests/test_packaging_homebrew.py tests/test_readme_docs.py -q --no-cov` passed (`221 passed`), `bash -n packaging/build_linux.sh packaging/build_windows.sh` passed, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest publish/sync-security verification: red tests caught PyPI `workflow_dispatch` being able to reach the publish job, PyPI tag publishing missing built-wheel install smoke, sync bridge executing `mcp_call` / browser writes / `computer_*` without host-loop approval, and evaluator exceptions fail-opening writes/computer actions. After fixes, `uv run pytest tests/test_packaging_pypi.py tests/test_packaging_release_workflow.py tests/test_packaging_homebrew.py tests/test_packaging_winget.py tests/test_broker_execute_sync.py tests/test_broker_computer_artifact.py tests/test_broker_request_blocking.py tests/test_write_file_broker_gate.py tests/test_approval_levels.py::test_cautious_auto_passes_sandboxed_run_command tests/test_egress_valve.py -q --no-cov` passed (`125 passed, 1 deselected`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest permissions/release/hooks verification: red tests caught stale per-run permissions config after reload, suffix-wide release asset collection, and invisible failed `UserPromptSubmit` hooks. After fixes, `uv run pytest tests/test_app_factory.py tests/test_tui_permissions_cmd.py tests/test_permissions_config.py tests/permissions/test_l0_l2_semantics.py tests/daemon/test_p3_approval_circuit.py tests/test_packaging_release_workflow.py tests/test_packaging_pypi.py tests/test_packaging_homebrew.py tests/test_packaging_winget.py tests/test_tui_hooks_cmd.py tests/test_hooks_config.py tests/test_hooks_runner.py tests/e2e/test_loop_hooks.py tests/test_activity_panel.py -q --no-cov` passed (`255 passed, 2 deselected`), `uv run pytest tests/test_release_workflow.py tests/test_packaging_release_workflow.py -q --no-cov` passed (`16 passed`) after aligning legacy release workflow tests with exact manifests, `uv run pytest -m "not slow" --no-cov` passed (`4873 passed, 16 skipped, 37 deselected`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest daemon multi-turn context verification: red tests caught daemon runs lacking a persisted owner session and `RunWorker` calling `AgentLoop.run()` with `run-<run_id>`; after the fix, `uv run pytest tests/test_daemon_worker.py::test_worker_uses_run_conversation_session tests/test_daemon_server.py::test_create_run_persists_owner_session_for_context -q --no-cov` passed, `uv run pytest tests/test_daemon_worker.py tests/test_daemon_server.py tests/test_tui_daemon_client_mode.py tests/daemon/test_resume_suspended.py -q --no-cov` passed (`48 passed`), `uv run pytest tests/daemon/test_kernel_powerup.py -q --no-cov` passed (`9 passed`), `uv run pytest tests/daemon/test_p3_approval_circuit.py::test_concurrent_runs_approval_isolation -q --no-cov` passed, `uv run argos --selftest` passed, and `git diff --check` passed.
- Latest WinGet schema verification: red tests caught the root manifest using `ManifestType: defaultLocale`, the locale file missing `ManifestType`/`ManifestVersion`, and installer `UpgradeBehavior` using an invalid mapping; after the fix, `uv run pytest tests/test_packaging_winget.py tests/test_packaging_pypi.py::test_argospkg_manifest_fails_on_placeholder_winget_digest -q --no-cov` passed (`11 passed`), all three WinGet manifests validated against official 1.6 schemas with a temporary fake digest, `uv run argospkg manifest` still failed on `PLACEHOLDER_FROM_BUMP`, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest macOS release packaging verification: red test caught `packaging/build_arm64.sh` not stripping tag prefix versions and writing app `Info.plist` from `packaging/VERSION`; after the fix, `uv run pytest tests/test_install_script.py::test_build_arm64_uses_release_version_for_app_plist -q --no-cov` passed, `uv run pytest tests/test_install_script.py tests/test_packaging_release_workflow.py -q --no-cov` passed (`19 passed, 1 skipped, 1 deselected`), `bash -n packaging/build_arm64.sh packaging/install.sh` passed, source `packaging/Info.plist` still reported version `0.1.0`, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest launch-surface verification: red tests caught README and packaging docs still presenting many install channels and `release.yml` still auto-triggering on `v*` tags; after the fix, `uv run pytest tests/test_readme_docs.py::test_readme_launch_install_surface_is_small tests/test_readme_docs.py::test_packaging_doc_marks_channel_commands_as_planned tests/test_packaging_release_workflow.py::test_release_workflow_is_manual_for_binary_assets tests/test_release_workflow.py::test_workflow_is_manual_for_binary_release -q --no-cov` passed, `uv run pytest tests/test_readme_docs.py tests/test_packaging_release_workflow.py tests/test_release_workflow.py tests/test_packaging_pypi.py -q --no-cov` passed (`88 passed, 1 deselected`), YAML parsing confirmed `release.yml` has only `workflow_dispatch` while `publish.yml` keeps tag publishing, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest curl-installer verification: red tests caught the missing root `install.sh` and missing README curl command; after the fix, `uv run pytest tests/test_install_script.py::test_root_install_script_bootstraps_through_uv_tool_only tests/test_readme_docs.py::test_readme_launch_install_surface_is_small -q --no-cov` passed, `uv run pytest tests/test_install_script.py tests/test_readme_docs.py -q --no-cov` passed (`57 passed, 1 skipped, 1 deselected`), `bash -n install.sh packaging/install.sh` passed, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest launch-install hardening verification: red tests caught root `install.sh` depending on `argos` already being on `PATH`, old `packaging/install*.sh` looking like live public installers, and missing PyPI job gate assertions. After fixes, `uv run pytest tests/test_install_script.py tests/test_packaging_install_scripts.py tests/test_packaging_pypi.py tests/test_readme_docs.py -q --no-cov` passed (`90 passed, 1 skipped, 2 deselected`), `rm -rf dist && uv build && uv run --with twine twine check dist/*` passed, clean-wheel install smoke passed via `uv run pytest tests/test_packaging_pypi.py::test_pip_install_wheel_and_run_argos_version -q --no-cov -m slow`, `bash -n install.sh packaging/install.sh packaging/install-deb.sh` passed, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest raw-GitHub installer tracking verification: red test caught `install.sh` missing from `git ls-files`; after staging only `install.sh`, `uv run pytest tests/test_install_script.py::test_root_install_script_is_tracked_for_raw_github_install -q --no-cov` passed, `uv run pytest tests/test_install_script.py tests/test_packaging_install_scripts.py tests/test_readme_docs.py -q --no-cov` passed (`66 passed, 1 skipped, 1 deselected`), `git ls-files --error-unmatch install.sh` returned `install.sh`, `bash -n install.sh packaging/install.sh packaging/install-deb.sh` passed, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest public security wording verification: red test caught README/SECURITY over-claiming broker coverage for raw CodeAct Python while the OS sandbox is default-off; after the wording fix, `uv run pytest tests/test_readme_docs.py::test_public_security_docs_do_not_overclaim_broker_for_raw_codeact tests/test_readme_docs.py::test_readme_sandbox_docs_distinguish_codeact_from_broker_web tests/test_readme_docs.py::test_security_doc_uses_config_dir_and_current_sandbox_names -q --no-cov` passed, full `uv run pytest tests/test_readme_docs.py -q --no-cov` passed (`49 passed`), `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest Linux required package asset verification: red tests caught `packaging/build_linux.sh` warning and continuing when `.deb` / `.rpm` required assets were missing or rpm renaming failed; after the fix, `uv run pytest tests/test_packaging_linux_spec.py::test_build_linux_script_fails_when_required_package_assets_are_missing -q --no-cov` passed, `uv run pytest tests/test_packaging_linux_spec.py tests/test_packaging_release_workflow.py -q --no-cov` passed (`20 passed`), `bash -n packaging/build_linux.sh` passed, `uv run argos --selftest` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest uv-bootstrap installer verification: red test caught root `install.sh` requiring preinstalled `uv`; after the fix, `uv run pytest tests/test_install_script.py::test_root_install_script_bootstraps_uv_when_missing tests/test_install_script.py::test_root_install_script_bootstraps_through_uv_tool_only tests/test_install_script.py::test_root_install_script_verifies_installed_argos_without_path -q --no-cov` passed, `uv run pytest tests/test_install_script.py tests/test_readme_docs.py -q --no-cov` passed (`62 passed, 1 skipped, 1 deselected`), `bash -n install.sh packaging/install.sh packaging/install-deb.sh` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest release cleanup verification: after explicit user confirmation, `gh release delete v0.1.0 --repo tungoldshou/argos --cleanup-tag --yes` succeeded; `gh release view v0.1.0 --repo tungoldshou/argos --json tagName,url` now returns `release not found`, `git ls-remote --tags origin refs/tags/v0.1.0` returns no tag, PyPI/TestPyPI still return HTTP 404 for `argos-agent`, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest TestPyPI workflow verification: red test caught `publish.yml` lacking a TestPyPI preflight job; after the fix, `uv run pytest tests/test_packaging_pypi.py -q --no-cov` passed (`26 passed, 1 deselected`), `uv run pytest tests/test_packaging_pypi.py tests/test_readme_docs.py::test_packaging_doc_marks_channel_commands_as_planned -q --no-cov` passed (`27 passed, 1 deselected`), `bash -n install.sh packaging/install.sh packaging/install-deb.sh` passed, `git diff --check` passed, and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest fresh-version verification: after bumping the public launch target to `0.1.1`, `rm -rf dist && uv build` produced `dist/argos_agent-0.1.1.tar.gz` and `dist/argos_agent-0.1.1-py3-none-any.whl`; `uv run --with twine twine check dist/*` passed; `uv run argos --version` returned `argos 0.1.1`; the installed-wheel version smoke passed; and `CLAUDE.md` / `AGENTS.md.bak` were confirmed absent.
- Latest unpublished-release cleanup verification: `CHANGELOG.md` no longer contains old formal/pre-release sections or GitHub release links; the code-comment/docstring scanner returned `code_comment_docstring_chinese_findings=0`; `uv run python -m compileall -q argos tests scripts packaging smoke_packaged.py`, `git diff --check`, targeted packaging/docs tests, `rm -rf dist && uv build`, `uv run --with twine twine check dist/*`, and `uv run argos --version` all passed.
- Latest product-spine reset verification: on branch `codex/product-spine-reset`, default TUI `/help` and slash menu now show only core commands, advanced commands remain available via exact command and `/help advanced`, Space no longer triggers `/voice`, Dynamic Workflows are opt-in with `ARGOS_WORKFLOWS=1`, generic contract prompt injection was removed for ordinary code terms, persistent `always` rules are scoped or not saved, verification mutation detection uses successful file tool names/receipts before fallback, `/setup` renders a status card, public docs now present core-stable defaults plus advanced/experimental surfaces, and exact `"""Internal documentation."""` noise was removed repo-wide. Verification passed: `uv run pytest -m "not slow" --no-cov` (`4924 passed, 15 skipped, 37 deselected`), temp-config inline `uv run argos --selftest`, `rm -rf dist && uv build`, `uv run --with twine twine check dist/*`, `uv run python -m compileall -q argos tests scripts packaging smoke_packaged.py`, and `git diff --check`.
- Latest product-reset review pass: high-risk command surface, workflow/prompt, permissions, verify/file-tool, TUI approval/setup, and docs diffs were reviewed. A permissions review fix tightened `run_command` persistent allow matchers so commands with leading flags save the exact token prefix through the first non-flag argument instead of degenerating to a bare command matcher. Verification passed: `uv run pytest tests/test_approval.py tests/test_egress_valve.py tests/test_tui_approval_trigger.py tests/test_inline_choice.py -q --no-cov` (`89 passed`) and the combined product-reset slice (`856 passed`).
- Latest post-review verification: `uv run pytest -m "not slow" --no-cov` passed (`4924 passed, 16 skipped, 37 deselected`), temp-config inline `uv run argos --selftest` passed, `uv run python -m compileall -q argos tests scripts packaging smoke_packaged.py` passed, `git diff --check` passed, exact `"""Internal documentation."""` search returned no matches, `rm -rf dist && uv build` produced `argos_agent-0.1.1` wheel/sdist, and `uv run --with twine twine check dist/*` passed. External state remains unchanged: PR #29 is open/non-draft/CLEAN with CI success, while PyPI/TestPyPI `argos-agent` still return 404 until trusted publishing/release is completed.
- Latest PR merge closeout: PR #29 (`codex/ship-readiness`) merged to `main` with merge commit `3c966d8`, and PR #30 (`codex/product-spine-reset`) merged to `main` with merge commit `5e714e9`. A `main` `publish.yml` workflow dispatch passed the Linux+bwrap CI gate and distribution build/twine/wheel-smoke jobs, then failed only at TestPyPI OIDC trusted publishing with `invalid-publisher` for `repo:tungoldshou/argos:environment:testpypi`, workflow `.github/workflows/publish.yml`, ref `refs/heads/main`. PyPI/TestPyPI still return 404 for `argos-agent` until the TestPyPI pending publisher is configured and the publish workflow is rerun.
- Latest installer direction: the public one-line install path now uses the versionless raw GitHub `main/install.sh` URL but installs `argos-agent` from GitHub's latest release tag through `uv tool install --force`; `ARGOS_INSTALL_REF` remains available for explicit tag/commit/main overrides.
- Latest installer verification: targeted installer/readme/packaging tests passed (`72 passed, 1 skipped, 1 deselected`), temp-config `uv run argos --selftest` passed, `git diff --check` passed, and shell syntax checks passed. Root `install.sh` now resolves the latest release before bootstrapping `uv`, and fails cleanly with an `ARGOS_INSTALL_REF=main` hint when no release exists.
