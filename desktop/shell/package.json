{
  "name": "argos-shell",
  "version": "0.1.0",
  "description": "Argos desktop shell — Tauri 2 walking skeleton",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-from-sdk": "node ../sdk/scripts/sync_to_shell.mjs",
    "check": "tsc --noEmit",
    "build": "tsc --noEmit && echo 'frontend type-check OK (static HTML; no bundler needed for skeleton)'",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@tauri-apps/api": "^2",
    "typescript": "^5.4.0",
    "@types/node": "^25.9.3"
  }
}
