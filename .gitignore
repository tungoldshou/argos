node_modules
dist
dist-ssr
*.local
.DS_Store
*.log
.vite

# tauri / rust
src-tauri/target
src-tauri/gen

# local env (dev keys — never commit)
.env.local
.env*.local

# codegraph index + tsc build cache
.codegraph
*.tsbuildinfo
.claude/
